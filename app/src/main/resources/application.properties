spring.application.name=app

# 本地端口等见 application-local.properties，启动时加 profile：local（IDE launch 已带）

# MySQL（由 infra 引入驱动）：本地或部署时配置，例如：
# spring.datasource.url=jdbc:mysql://127.0.0.1:3306/your_db?useSSL=false&characterEncoding=utf8
# spring.datasource.username=
# spring.datasource.password=
