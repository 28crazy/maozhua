spring.application.name=client
server.port=${SERVER_PORT:80}
# DB / Redis 由 infra 模块资源统一提供，经 classpath 导入
spring.config.import=optional:classpath:mysqlConfig/mysql.properties,optional:classpath:redisConfig/redis.properties